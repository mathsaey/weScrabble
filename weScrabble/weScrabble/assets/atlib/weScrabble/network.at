import /.at.lang.futures;
import ~.game;

/**
TODO: 
	- game implementatie files naar aparte map verplaatsen
	- game.newPlayerInstance naar netwerk code verplaatsen
	- generale code maken om games te importeren
	- games mergen
*/


/*************       
 * Exporter  *
 *************/
def exp := object: { 
	def localPlayer := player.new("Vinz",1);
	def game := game.new("VinzGame!", ["Vinzteam!", "not vinz team :("], localPlayer);

	def export() {
		export: game as: Game;
	};
};

/*************       
 * Importer  *
 *************/

 def imp := object: { 
 	def localPlayer := player.new("Kenny", 2);
 	def joinedGame;

 	def join() {
 		whenever: Game discovered: {|game| 
 			system.println("Game discovered");
 			joinedGame := game<-newPlayerInstance(localPlayer,localPlayer.teamIdx())@TwoWay;
 			when: joinedGame becomes: {|game|
 				system.println("Game resolved");
 				joinedGame := game;
 			};
 		};
 	};
 };

network.online();
self;